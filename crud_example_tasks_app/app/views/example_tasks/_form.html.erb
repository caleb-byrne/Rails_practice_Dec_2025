<%= form_with(model: example_task) do |form| %>
  <% if example_task.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(example_task.errors.count, "error") %> prohibited this example_task from being saved:</h2>

      <ul>
        <% example_task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
